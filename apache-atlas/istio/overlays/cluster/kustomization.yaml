apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target:
      kind: Service
      name: istio-ingressgateway-atlas
      namespace: istio-system
    patch: |-
      - op: replace
        path: /spec/type
        value: LoadBalancer

